import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'mapquest-app',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `<h1>MapQuest</h1>`
})
export class AppComponent {

}
